window.addEventListener("DOMContentLoaded", function (){

    const exibeNumero = (numero) => alert(numero);

    const dobroTriplo = (numero) => {
        this.alert(numero*2);
        this.alert(numero*3);
    }

    // dobroTriplo(43);

    const dobro = numero => numero * 2;

    this.alert(dobro(5));
});